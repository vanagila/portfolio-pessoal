import styled from "styled-components";

export const Container = styled.div`
  button {
    position: fixed;
    bottom: 15px;
    right: 1px;
    background: transparent;
    border: none;
    color: var(--green);
  }
`;
